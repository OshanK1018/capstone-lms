.course-navbar {
    padding: 18px 25px 5px;
    background: #f6f8fb;
}

.course-navbar__container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 18px 20px;
    background: #ffffff;
    border: 1px solid #e1e7ef;
    border-radius: 14px;
    box-shadow: 0 2px 8px rgba(15, 23, 42, 0.04);
}

.course-navbar__top {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 24px;
    margin-bottom: 14px;
}

.course-navbar__label {
    margin: 0 0 4px;
    color: #3b82f6;
    font-size: 0.73rem;
    font-weight: 800;
}

.course-navbar__top h1 {
    margin: 0;
    color: #1e293b;
    font-size: 1.25rem;
    font-weight: 800;
}

.course-navbar__top select {
    height: 38px;
    min-width: 290px;
    padding: 0 12px;
    border: 1px solid #dce3ed;
    border-radius: 8px;
    background: #f8fafc;
    color: #334155;
    font-size: 0.78rem;
    font-weight: 700;
}

.course-navbar__top select:focus {
    outline: none;
    border-color: #60a5fa;
}

.course-navbar__links {
    display: flex;
    align-items: center;
    gap: 32px;
    padding-top: 12px;
    border-top: 1px solid #edf2f7;
}

.course-navbar__links a {
    position: relative;
    padding: 8px 4px 13px;
    color: #64748b;
    font-size: 0.78rem;
    font-weight: 700;
    text-decoration: none;
}

.course-navbar__links a:hover,
.course-navbar__links a.active {
    color: #3b82f6;
}

.course-navbar__links a.active::after {
    content: "";
    position: absolute;
    left: 50%;
    bottom: 2px;
    width: 26px;
    height: 3px;
    background: #3b82f6;
    border-radius: 20px;
    transform: translateX(-50%);
}

@media (max-width: 650px) {
    .course-navbar {
        padding: 15px 18px 5px;
    }

    .course-navbar__top {
        align-items: flex-start;
        flex-direction: column;
        gap: 14px;
    }

    .course-navbar__top select {
        width: 100%;
        min-width: 0;
    }

    .course-navbar__links {
        gap: 20px;
        overflow-x: auto;
    }

    .course-navbar__links a {
        white-space: nowrap;
    }
}